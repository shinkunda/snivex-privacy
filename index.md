# Snivex プライバシーポリシー ／ Snivex Privacy Policy

最終更新日: 2026年9月2日 ／ Last updated: September 2, 2026

---

## 日本語

### 1. はじめに

Snivex（以下「本拡張機能」）は、ウェブページのスクリーンショットを撮影し、
タグとフォルダで整理して、あとから見つけられるようにするための、
Google Chrome ／ Microsoft Edge 向けのブラウザ拡張機能です。

本ポリシーは、本拡張機能が利用者の情報をどのように扱うかを説明します。

**本拡張機能は、利用者のアカウント登録・ログインを一切必要としません。**

### 2. 本拡張機能が扱う情報

本拡張機能が扱う情報は、次のものだけです。

| 情報 | いつ作られるか | 保存場所 |
|---|---|---|
| スクリーンショット画像 | 利用者が撮影操作を行ったとき | 利用者の端末内 |
| 撮影したページのタイトル | 利用者が撮影操作を行ったとき | 利用者の端末内 |
| 撮影したページのアドレス（URL） | 利用者が撮影操作を行ったとき | 利用者の端末内 |
| 撮影した日時 | 利用者が撮影操作を行ったとき | 利用者の端末内 |
| タグ名・フォルダ名 | 利用者が入力したとき | 利用者の端末内 |
| 注釈（矢印・文字・図形など） | 利用者が注釈を付けたとき | 利用者の端末内 |
| 設定（撮影後の動作・保存先・外観・言語・音量など） | 利用者が設定を変更したとき | 利用者の端末内 |
| 撮影に成功した回数 | 撮影が成功したとき | 利用者の端末内 |
| 録画した動画（タブの映像・タブの音声） | 利用者が録画操作を行ったとき | 利用者の端末内 |
| マイクの音声（利用者が有効にした場合のみ） | 利用者がマイクを有効にして録画したとき | 利用者の端末内 |
| フィードバックの回答（第5章） | 利用者がフィードバックを送ったとき | 第5章に記載 |

#### タイトル・アドレス・日時について

この3つがあるから、Snivex は数か月後でも撮ったものを見つけられます。
検索の対象であり、PCへ保存するときのファイル名の一部であり、
詳細画面のタイトルは撮影元のページへ戻るリンクになっています。

- 記録されるのは、利用者が自分で撮影したページの分だけです。閲覧しただけのページは1件も残りません。本拡張機能は閲覧を見張っていません
- 画像と一緒に、利用者の端末内（拡張機能用のストレージ）に保存されます
- 外部へ送信されることはありません。フィードバック（第5章）にも含まれません
- キャプチャを削除すれば、この3つも一緒に消えます

#### 取得しないもの

本拡張機能は、次の情報を取得しません。

- 氏名・メールアドレス・住所・電話番号などの個人を特定する情報
- 撮影していないページの記録（閲覧しただけのページの履歴）
- Google アカウントの情報
- 位置情報
- 利用者を横断的に追跡するための識別子（トラッキングID）

#### タブの録画について

Snivex には、利用者の操作でタブを録画する機能があります（最大30秒）。

- 利用者が録画を開始したときだけ動作します。バックグラウンドで自動的に録画することはありません
- 録画には、そのタブの映像が含まれます。タブの音声が含まれる場合があります
- マイクの音声は、利用者が設定でマイクを有効にした場合だけ録画に含まれます。マイクは既定でオフです
- 録画した動画は、利用者の端末内で処理され、動画ファイルとして利用者のパソコンへ保存されるだけです。運営者のサーバーを含む外部へ送信されることはありません（第5章）
- 録画は Snivex のギャラリーには記録されません。撮影した画像とは扱いが異なり、録画した動画はブラウザのダウンロード機能を通じて直接ファイルとして保存されるため、削除する場合は利用者自身がパソコン上のそのファイルを削除してください

### 3. スクリーンショット・録画データの扱い

撮影した画像と、一緒に記録されるタイトル・アドレス・日時は、
利用者の端末内（ブラウザの拡張機能用ストレージ）にのみ保存されます。
録画した動画は、ギャラリーには記録されず、ブラウザのダウンロード機能を通じて
直接、利用者のパソコンへ動画ファイルとして保存されます（第2章）。

- 運営者を含む第三者が、これらを閲覧することはできません
- インターネット上へ送信されることはありません
- 利用者が「PCへ保存」や「クリップボードへコピー」を選んだ場合は、利用者自身の操作として、利用者の端末内でのみ処理されます

#### 撮影内容に写り込む情報について

撮影した範囲や画面には、結果として利用者自身の個人情報や、パスワードなどの機微な
情報が写り込む場合があります。これは利用者がどの範囲を、いつ撮影したかによるもので、
本拡張機能があらかじめ判断できるものではありません。本拡張機能は、撮影した画像・
動画の中身を解析したり、その内容に基づいて何かを判断したりすることは一切ありません。
撮影されたとおりに、利用者の端末内へそのまま保存されるだけです。

### 4. 権限について

本拡張機能は、次の権限を要求します。

- **activeTab** — 利用者が撮影を開始したタブの内容を取得し、そのページのタイトルとアドレスをキャプチャと一緒に記録するため
- **scripting** — 撮影範囲の選択と注釈の編集画面をページ上に表示するため
- **storage / unlimitedStorage** — 画像と設定を端末内に保存するため
- **downloads** — 画像・録画データを利用者のパソコンへ保存するため
- **clipboardWrite** — 画像をクリップボードへコピーするため
- **offscreen** — 撮影完了時の効果音を再生するため、および録画データを端末内で処理するため
- **tabCapture** — 利用者が明示的に録画を開始した場合にのみ使用します。現在のタブの映像・音声を取得するためのもので、利用者の操作なしに録画が始まることはありません

#### すべてのサイトへのアクセスについて

本拡張機能は、インストール時にすべてのサイトへのアクセスを要求しません。

ページ全体の撮影で、ページ内に埋め込まれた別サイトの領域（iframe）まで撮る必要が
生じた場合にのみ、そのサイトについてだけ、利用者に許可を求めます。
許可しなくても、その部分を除いた撮影は行えます。

### 5. 外部への送信

本拡張機能が外部へ情報を送るのは、利用者が自らフィードバックを送ったときだけです。

そのとき送られるのは、次の6項目だけです。

1. 選んだ評価（「とても便利」など）
2. 利用者が書いた文章（任意。書かなければ空です）
3. どちらの質問に答えたか
4. 答えた日時
5. Snivex のバージョン
6. 表示言語（日本語／英語）

送られないもの: スクリーンショット、録画した動画（タブの映像・タブの音声・マイクの
音声を含む）、撮影したページのタイトル・アドレス、閲覧したページの情報、
氏名・メールアドレス、Google アカウントの情報、利用者を特定する識別子。

送信先は運営者が管理するサーバー（Vercel）およびデータベース（Neon）です。

受け取ったフィードバックを保存するデータベースには、IP アドレスも User-Agent も
記録しません（保存する項目は上記の6項目のみで、データベースにそれらの項目自体が
存在しません）。なお、サーバーを提供する事業者（Vercel）が、通信の記録
（アクセスログ）を一定期間保持する場合がありますが、これは本拡張機能が
取得・保存するものではありません。

フィードバックは、送るかどうかを利用者が選べます。送らなくても、本拡張機能の
すべての機能をそのまま利用できます。また、送る前に「何が送られるか」を画面に
表示します。

#### 解析・広告について

解析ツール（アクセス解析）や広告のためのプログラムは、一切組み込まれていません。
設定画面にある AI サービス（ChatGPT・Claude・Gemini など）へのショートカットは、
既定で無効になっており、利用者が押したときに、そのサービスのページを新しいタブで
開くだけです。本拡張機能がスクリーンショットやその他の情報をこれらのサービスへ
送信することはありません。

### 6. 第三者への提供

本拡張機能は、利用者の情報を第三者へ販売・提供しません。
また、本拡張機能の中核機能と無関係な目的で利用・移転することもありません。

### 7. 広告・分析

広告は表示しません。アクセス解析ツールも組み込んでいません。

### 8. データの削除

- 個別の削除 — ギャラリー画面から、画像を1件ずつ、または選択してまとめて削除できます。削除すると、一緒に記録されたタイトル・アドレス・日時も消えます
- すべての削除 — ブラウザの「拡張機能を削除」を行うと、本拡張機能が端末内に保存したデータはすべて削除されます
- 送信済みのフィードバックについて — フィードバックには利用者を特定する情報が含まれないため、送信後に個別の回答を特定して削除することはできません。この点を踏まえ、送る内容は利用者自身がご確認ください（送信前に何を送るかを画面に表示します）

### 9. データの保存場所

本拡張機能は `chrome.storage.local` を使用します。これは Chrome と Microsoft Edge の
どちらもが提供する拡張機能用の保存領域で、利用者の端末内にあります。

ブラウザへのサインイン（Chrome では Google アカウント、Edge では Microsoft アカウント）を
通じて他の端末と同期される `chrome.storage.sync` は使用していません。

### 10. セキュリティ

- 外部から実行時にプログラムを取得して動かす仕組み（リモートコード）はありません
- すべてのプログラムは拡張機能のパッケージ内に同梱されています
- フィードバックの送信には HTTPS を使用します

### 11. Chrome Web Store のポリシーへの準拠

本拡張機能によるユーザーデータの使用および第三者への提供は、Limited Use の要件を
含む Chrome Web Store ユーザーデータポリシーに準拠します。

### 12. 子どものプライバシー

本拡張機能は、年齢に関する情報を取得しません。
また、個人を特定する情報を取得しないため、特定の年齢層を対象とした処理も行いません。

### 13. ポリシーの変更

本ポリシーは、本拡張機能の機能変更に応じて更新されることがあります。
新しい種類のデータを外部へ送り始める場合など、重要な変更を行うときは、
本ページの最終更新日を変更してお知らせします。

### 14. お問い合わせ

本ポリシーまたは本拡張機能に関するお問い合わせは、次までご連絡ください。

- メールアドレス: iveli.official@gmail.com
